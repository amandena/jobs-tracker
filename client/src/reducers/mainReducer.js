const initialSearchState = {
  searchBox: ''
}

export default function mainReducer(state = {
  searchForm: initialSearchState
}, action) {
  switch (action.type) {
    default:
      return state
  }
}